package com.ecomap.ecomap

import androidx.fragment.app.Fragment

/**
 * Simple fragment to contain the SupportMapFragment. A fragment for managing the lifecycle of a
 * GoogleMap object.
 */
class MapFragment : Fragment()
